pipeline {
    agent any

    stages {
        stage('Compile') {
            steps {
                echo 'Compile'
            }
        }

    }
    post {
        always {
            echo 'I will always say Hello again!'
        }
    }

}
